import styled from 'styled-components';

export const ButtonLoadmoreContainer = styled.button`
    width: 100%;
    background-color: var(--color-highlight);
    color: #fff;
    text-transform: uppercase;
    border: none;
    padding: 1rem;
    cursor: pointer;

`